<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meuimc"
        tools:targetApi="31">

        <!-- Tela principal do app (boas-vindas + botão iniciar) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de cálculo de IMC -->
        <activity
            android:name=".CalculoIMCActivity"
            android:exported="true" />

        <!-- Telas de resultado por classificação do IMC -->
        <activity android:name=".AbaixoDoPesoIdealActivity" android:exported="true" />
        <activity android:name=".PesoNormalActivity" android:exported="true" />
        <activity android:name=".SobrepesoActivity" android:exported="true" />
        <activity android:name=".ObesidadeGrau1Activity" android:exported="true" />
        <activity android:name=".ObesidadeGrau2Activity" android:exported="true" />
        <activity android:name=".ObesidadeGrau3Activity" android:exported="true" />

    </application>

</manifest>
